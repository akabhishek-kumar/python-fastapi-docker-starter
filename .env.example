# Copy this file to .env and fill in your values
# .env is gitignored — never commit real secrets

# PostgreSQL connection string
DATABASE_URL=postgresql+asyncpg://postgres:yourpassword@localhost:5432/fastapi_starter

# App settings (optional — defaults are fine for dev)
APP_NAME=FastAPI Starter
DEBUG=False
